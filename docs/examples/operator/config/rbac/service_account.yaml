# The identity the controller authenticates as. Every RBAC rule in this
# directory is bound to this one ServiceAccount.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tasklane-operator
  namespace: tasklane-system
  labels:
    app.kubernetes.io/name: tasklane-operator
    app.kubernetes.io/part-of: tasklane
