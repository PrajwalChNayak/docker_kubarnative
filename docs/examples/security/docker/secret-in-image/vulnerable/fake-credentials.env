# THIS FILE IS A DELIBERATELY FAKE CREDENTIAL used only to demonstrate a
# security anti-pattern in a disposable lab. It is not a real secret and
# grants access to nothing. Never put a real credential in build context.
FAKE_API_KEY=demo-not-real
FAKE_DB_PASSWORD=demo-not-real-either
